rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Každý uživatel vidí a mění jen svá data.
    match /users/{uid}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
